import UniformTypeIdentifiers

extension UTType {
    /// Runimation's portable file format: a run + its full visualisation config as JSON.
    static let runi = UTType(exportedAs: "me.tmbr.runimation.runi")
}
